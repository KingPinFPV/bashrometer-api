# Port for the API server to listen on
PORT=3000

# Full connection string for your PostgreSQL database
# Example for local Docker PostgreSQL: postgresql://youruser:yourpassword@localhost:5432/bashrometer_db
# Example for cloud PostgreSQL provider: postgresql://user:password@host:port/database?sslmode=require
DATABASE_URL=

# Secret key for signing and verifying JSON Web Tokens (JWTs)
# Should be a long, random, and strong string in production
JWT_SECRET=

# Optional: Node environment (development, production, test)
# NODE_ENV=development